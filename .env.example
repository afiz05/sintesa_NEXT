# Captcha Configuration
NEXT_PUBLIC_LOCAL_CEKMODE=http://localhost:3001/api/captcha-mode
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=your_recaptcha_site_key_here

# Note: 
# - NEXT_PUBLIC_LOCAL_CEKMODE should return { "capcay": "0" } for custom captcha or { "capcay": "1" } for reCAPTCHA
# - If the API is not available, the system will fallback to custom captcha mode ("0")
